<p>Welcome, <%= @resource.user.name %>!</p>

<p>You can confirm your email address by clicking the link below:</p>

<p><%= link_to 'Confirm my email', confirmation_url(@resource, :confirmation_token => @token) %></p>
