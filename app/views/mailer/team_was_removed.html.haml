- if @owner == @recipient
  - owner = link_to 'You', @owner_url
- else
  - owner = link_to @owner.name, @owner_url

- message = t "mailer.#{action_name}.message",
            owner: owner,
            name: @object.name

= message.html_safe
