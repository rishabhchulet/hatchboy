- if @owner == @recipient
  - message = "#{link_to('You', @owner_url)} have just created new team '#{link_to @object.name, team_url(@object)}'"
- else
  - message = "#{link_to(@owner.name, @owner_url)} has just created new team '#{link_to @object.name, team_url(@object)}'"

= message.html_safe
