.grid-body.no-border
  - if list_marked.present?
    %table.table.table-hover.table-bordered.no-more-tables.action-list#teams-short-list
      %thead
        %tr
          %th #
          %th Description
          %th Total Amount
          %th Created
          %th Creator
          %th Actions
      %tbody
        =render partial: "payments/row_marked", collection: list_marked
  - else
    %p There are no payments yet
