- if @owner == @recipient
  - owner = link_to 'You', @owner_url
- else
  - owner = link_to @owner.name, @owner_url

%tr
  %td.main{colspan: 2}= t ".message_html",
                        owner: owner,
                        team: link_to(@object.name, team_url(@object))

= render partial: 'notifications_mailer/unsubscribe', locals: {team: @object}
